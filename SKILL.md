---
name: haohao-shuohua
description: 中文写作常驻清洗规范，保事实、去 AI 味、加中文味。写文档、周报、通知、方案、回复、总结、润色或交付 AI 起草内容时调用；纯翻译、代码和接口文档技术改写除外。
---

# 好好说话 · 中文写作底层清洗

**这是一套常驻的写作规范。** 只要产出中文，就默认从这里过一次，让文字回到「像人说的中文」。

一句话总纲：**保事实不动，去 AI 味，加中文味，不许造词充深刻。**

> 轻档（回消息、随手一段）看这一页够了。中重档（手记、硬文）的完整流程、两轴配置、report 模板在 `references/workflow.md`。

---

## 使用面（先看这里）

- **默认常驻**：任何写中文的场景，模型都应该把本技能的原则和红线内化到起草过程里，不要等用户喊「改稿」再启动。用户说「写篇文档」「起草个方案」「帮我写段周报」，就已经命中本技能。
- **显式触发**：用户主动喊「过一遍好好说话」「改成中文的样子」「这段像 AI」，进中档或重档，跑完整流程。
- **被上层调用**：任何上层写作技能（想法硬文、畅想、技术科普等）在自己的「去 AI 味/收尾清洗」环节，都应该把成稿交本技能重档过一次，省得各自维护一份会走样的 AI 味清单。依赖是单向的，上层调用它，它不反过来依赖上层。

### 自我豁免：说明书的条目免检，说明书里的话要过

本技能自己就是规范文本，规范文本里有大量编号、量词标题、并列条目（「四条原则」「三档场景」「第五层」）。**这些是工具说明书的可执行索引，不受文章红线约束，免检。** 判据只有一条：**它是「条目」还是「话」。** 编号、锚点、清单项是给人查的条目，留着；一旦是讲给人听的整句叙述，就得过红线，不许有翻译腔和表演腔。一句话：**目录随便编号，正文老实说话。**

---

## 四条原则（全档共享，顺序即优先级）

### 一、保事实（最高，死线）

改中文的过程里，**原意、数字、命令、术语、谓词方向、效果类型、关系类型、硬机制描述、论文级证据，一律不许动**。听着再顺，把「压力大→辞职多」改成「辞职多→压力大」就是废稿。

**保真高于加中文味**：精确机制（如「attention 是 Q-K 相关度加权」）不许被顶替成一个具体场景。论文结论不许简化成「有研究发现」。想加比喻，叠在机制后面，不许替换。

死线靠**关系账本**兜底（账本 8 项 + 落地流程见 `references/workflow.md` 第一段。受保护跨度定义见 `references/protected-spans.md`）。

### 二、去 AI 味

翻译腔加表演腔的组合病。**主自检 = 症状库五层扫描**（标点/句式/词汇/结构/结构排比），机械可枚举，命中即改。扫不出但读着别扭的疑难句，再用**回译测试**兜底（默读翻英文：一次成型 = AI 味，别扭需意译 = 中文）。清单和例子全在 `references/symptom-dictionary.md`。

**密度红线**：某段命中超过 5 处，单点改救不回来，整段重写（重档）或整段进删除清单（中档）。

#### 第五层扫描：结构排比病（强约束）

RLHF 训练出来的模型特别喜欢生成有编号感、有对仗感的内容，看着条理清晰，读着像机器打印出来的。本层抓两大族。**核心判据：数量和对仗本身不是判断，只是形式。用数量和对仗给内容贴标签，就是排比病。**

- **显性族**：A1 量词标题序列（「第一刀·第二刀」「三板斧」「五个维度」）；A2 分号或破折号串三连及以上子句。
- **隐性族**（更容易漏）：B1「X 不 X」三连以上；B2「不是 X 而是 Y」变体族（「这不是 X」「A 不是病，B 才是病」「管的是 A 不管 B」）；B3 二连对仗（「就该 A，就该 B」一段两处以上）；B4 段内三连罗列的名词/动词串。

**统一判据**：把这几个并列项都盖住，只留一个最要紧的判断，读者理解会不会少？不会 = 排比病，删或改陈述句。会 = 保留，节奏上补一处停顿。**A1/A2/B1–B4 的反例、改法、改后详例见 `references/symptom-dictionary.md` 第五层。**

**触发反应**：命中即标出，中档进「建议删除清单」，重档直接动刀。

### 三、加中文味

减完稿子会瘦一圈，可能瘦成空壳。加法把中文本来的写法补回来，落成四个问题（原理和标杆见 `references/chinese-four-principles.md`）：

1. **动词扛住这句没？** 动词是「进行/实现/完成/导致/构成/属于/存在/具有」这类没肉的，回炉，把真动作拎出来。
2. **有具体顶替抽象没？** 「效果显著」不如「页面打开从 3 秒降到 800 毫秒」。找不到具体，那句判断本身没想清楚。
3. **有喘不上气的长句没？** 念一遍，一口气顶到底、中途倒不上气 = 憋气句。
4. **段末多说读者已懂的话没？** 「综上所述/由此可见/换言之」，删。

**光看四问还是不会改的，去 `references/before-after-worktext.md`**：周报/汇报/方案/通知/答复五类工作文本的「AI 起草 → 修好」前后对照，每对标了改的动作。**学的是从左改到右那个动作，不是右边的文风**（右边只是把事说清的一种，别照抄腔调）。

**减法有底，节奏无顶。** 逐句查症状能削干净，顶多「没病」。「好」落在句与句之间（长短错落、停顿、调子），只能靠**念出声**。落成四个字面指标由机器粗筛，人念终审，细则见 `references/rhythm-check.md`。

**一条要先划死（最容易被带偏）：节奏管的是句子顺不顺、能不能喘上气，不管够不够口语。长句本身不算病，病在从头到尾没有一个停顿。首选补逗号断句，别急着拆句，更别掰成大白话。**

### 四、造词红线（死线）

**能用现成词就用现成词，禁止为了显得凝练自造新词。** AI 造的词一眼假、看着深刻、读者一脸懵。

硬约束：**一稿不超过一个自造词**。必须造的地方，同段内必须用一句大白话翻译一次（「XX，说白了就是……」）。判定五问、例外三条、常见造词模式表见 `references/word-coining-checklist.md`。

---

## 附加红线（和四条原则同优先级，单列因为最容易翻车）

### 比喻红线

**比喻只有一个合法用途：把一个读者本来不理解的概念讲明白。除此之外，一律不打比方。**

- 精确的东西直接说。「iOS 的动态性」「并发调度」「上下文窗口」「AST」有确切定义，直接用准确表述。**用不准确换准确，就是降专业性。**
- 门槛型比喻一律删。给外行降阅读门槛是科普/费曼式讲解的活，不是清洗中文的活。
- 比喻不承担论证。「这就好比……所以……」是拿类比顶替推理，回去补机理。
- 合法比喻也从省，全篇最多一两个当锚点。
- **例外**：面向外行的科普/费曼式讲解豁免。那种场景比喻是命根子，以它自己的规则为准。判断：读者是同行（硬约束）还是外行（科普豁免）？

### 反车轱辘话红线

haohao-shuohua 只在**句子级和段落级**做删除（「一节一观点」是结构级，留给写作技能）：

- **同义反复即删。** 判断：这句删掉，读者对观点的理解会不会少一分？不会，就是车轱辘话。
- **过渡句从省**（「顺着这个思路」「说到这里」）。**例外**：面向外行的科普/费曼式讲解里，「那么/于是/但是/所以」是逻辑链关节，不许删。
- **禁「铺垫—展开—小结」三段式。** 一句能说清的判断，直接讲。

### 姿态红线

- **禁**任何宣称式：「很多人没搞懂而我……」「大厂/硅谷都没想明白，但我……」「我早就看透了……」。
- **也禁**反方向的谄媚讨好：「好问题！」「您说得太对了」「这是一个很好的观点」「希望这对您有帮助」这类捧场垫话，是把聊天机器人腔当内容粘了进来。删干净，有话直接说。
- **只做**：呈现观察、摆机理、给证据，让读者自己得出「这人想明白了」。
- 平视。**两头都要平**：飘出一丝居高临下，或塌下去一味讨好，前面所有硬货都打折。

---

## 三档场景

| 档 | 触发 | 场景 | 力度默认 | Scope 默认 | 出 report? |
|---|---|---|---|---|---|
| **轻** | 常驻软自律，任何中文产出 | 回消息、口语转书面、群回复、随手一段 | minimal | in-place | 否，直接给正文 |
| **中** | 关键词触发 | 手记、周报、日报、短说明 | standard | bounded | 是 |
| **重** | 主动调用 / 被上层写作技能内部调用 | 硬文、对外文档、公众号稿 | standard | bounded（可升级） | 是 |

- **轻档**：起草和回消息时，四条原则和红线在心里过一遍，明显的 AI 味悄悄避掉或改掉，不解释、不列诊断，不跑完整流程。**但「心里过一遍」有个跳不过的硬底线：交付前至少机械扫三个零容忍项，命中即改，不许放过：**
  1. **「不是 X，而是 Y」及其变体**（「这不是……」「A 不是病，B 才是病」「管的是 A 不管 B」）：正着说，一个判断说完。
  2. **分号或破折号串三连及以上子句**：断开成独立句。
  3. **空心动词**（「进行/实现/完成/开展 + 名词」）：把真动作拎出来。
  这三条不靠"读着别扭"触发，是逐字扫，扫到就改。别的症状轻档可以睁只眼，这三条不行。
- **中档**：段级动刀，不擅自动结构。发现整段可删，列末尾「建议删除清单」交作者。
- **重档**：得到许可后可结构性重排，但必走三遍回读（细则见 workflow）。

**两轴独立**：力度轴（minimal/standard/aggressive）越大改得越狠。Scope 轴（in-place 只换词 / bounded 可拆合句子 / structural 可拆合段落改章节序）越大动的范围越大。可显式指定（「放开点改」= aggressive；「只挑最刺眼的」= minimal + in-place）。

**中重档的完整五段流程、bounded/structural 硬约束、三遍回读、report 模板、主线反思三问，全部见 `references/workflow.md`。**

---

## 标题红线（单列，最容易露 AI 味）

总要求：**平实、深刻，是什么就写什么。默认直给，把判断直接摆进标题，让读者一眼看到结论。** 要往公众号投、想留一点悬念也行，但底线是**「信」：标题许诺什么，正文就得给什么**。带钩子可以，空心钩子不行。

### 硬规则（踩一条就重起）

0. **直给优先（最高）。** 默认能一句话摆结论就摆，不绕、不藏。**例外**：投公众号等确需一点悬念的场合，可留钩子，但必须过下一条的「信」，勾起来的，正文照数兑现。
1. **禁破折号。** 标题出现「——」基本就是 AI 味，零容忍。
2. **禁超长。** 一行、约 20 字内。
3. **禁空心钩子。** 钩子本身不是病，勾了不兑现才是。「你绝对想不到」「真相是」「背后的秘密」这类只造悬念、正文给不出对应干货的，删。判据：标题许诺的信息，正文第一屏能不能兑现？兑现 = 合法钩子；兑现不了 = 空心钩子，删。
4. **禁「不是 X，而是 Y」及其变体族。** 头号 AI 味句式，标题零容忍。变体族包括「这不是 X」「A 不是病，B 才是病」「管的是 A 不管 B」「不在 A，而在 B」。
5. **禁三连罗列。** 「A、B、C」三名词堆成标题 = 逢三凑数无立场。
6. **一句陈述 + 一个判断。** 落地成一句平实陈述句，里面藏一个可被反驳的判断。
7. **章节标题不搞机械编号**（不用「一、二、三」），也不用「N刀/N步/N条」等量词做段落标题。数量不是判断，用数字给内容贴标签是结构排比病的隐蔽版。每个小标题自己就是一句能立住的话。
8. **标题也做回译测试。** 翻回英文一次成型 = 翻译腔。
9. **题眼落在读者已知的锚点上，别用陌生术语当题眼（最要命）。** 好奇心靠「你已经知道一点」勾起，不是「你完全不知道」。标题塞一个读者没听过的词，缺口太大，直接划走。修法：陌生术语撤下题眼，换成全文反复建立、读者一眼认得的概念，新东西留正文里揭。

**信达雅落地**：信 = 名副其实不骗点击（带钩子也要兑现）。达 = 一眼看懂不设门槛（关键词是已知锚点）。雅 = 留一个恰好的缺口（懂了大半还差一口）。**达和雅打架时达优先**，宁可直白，不为余味把话说到读者看不懂。

**章节小标题跟主标题不同**：主标题决定读者点不点进来，留个恰好的缺口没问题。章节小标题是读者已经在读了，不用再勾，直接陈述这一节的观点即可。别写「XX 那点别扭，根子是同一个」这类只卖关子、不给结论的句式。

### 标杆与反例

**对**：
- 「打开这个工程，和打开一个 txt 没区别」（直给一个反直觉判断，题眼都是已知词）
- 「AI 写代码为什么质量还是不太行」（朴素直问，不设扣子）
- 「『AI 味』的病根，一百年前就诊断过」（带一点钩子，但「病根」是读者已有的锚，缺口恰好，正文真的兑现了这个诊断）
- 「为了根除『AI 味』，我翻回了一百年前的语言学」（带钩子，留了「翻回语言学」的悬念，但正文照数兑现，是合法钩子的样板）

**错**：
- 「AI 说的中文，是欧化中文的工业版」。「是 X 的 Y 版」是英文骨架加钩子味。
- 「AI 味就是欧化中文，一百年前就诊断过」。**曾以为是标杆，其实踩了硬规则 9**：「欧化中文」是读者陌生术语，当题眼没人 get，缺口太大。换成读者已知的「病根」才成立。同一篇文章，只换题眼落在哪个词，标题就从「读不懂」变成「想读下去」。
- 「A 化、B 化、C 化：一次彻底的重构」。三连名词堆砌，无判断。
- 「这条路的尽头，是一个新深渊」。空心钩子，只造悬念，勾了不兑现。

---

## 使用速查

1. **判档**：起草和对话回复 → 轻档常驻。手记/周报/短稿 → 中档。硬文/对外 → 重档。
2. **锁账本**（中重档）：列关系账本 8 项（数字/命令代码术语专名/谓词方向/效果类型/关系类型/模态/硬机制/论文级证据）。详见 workflow。
3. **扫症状**：症状库五层扫一遍标密度（第五层专扫结构排比：显性族 A1 量词标题 / A2 分号串联，隐性族 B1「X 不 X」三连 / B2「不是 X 而是 Y」变体族 / B3 二连排比 / B4 段内三连罗列）。超过 5 处/段，重写或整段进删除清单。
4. **动刀**：按档位默认力度/scope，逐句查症状删改，加四个问题补中文味。bounded 下不删整句、不动结构。
5. **回读**（中重档）：Pass 1 对账本 / Pass 2 查残留 / Pass 3 跑节奏（憋气句、句长单调、软尾串、调子单一，只补停顿不口语化）。三遍分开不合并。
6. **反思三问**：主线是什么？节节服务吗？比改前清楚吗？（软性）
7. **出稿**：中重档给 report（模板见 workflow），轻档只出正文。

## 出稿前自检

- 事实账本对齐了吗（含硬机制/论文证据）？
- 症状库五层都扫过了吗？命中的都动刀了吗？四个问题答完了吗？
- **节奏跑过了吗？憋气句补停顿了吗（别拆好长句、别改口语）？**
- 有没有偷偷造新词？
- **比喻是不是都在讲清难懂概念，没有一个给精确概念套壳降门槛？**
- 有没有同义反复、废过渡句、铺垫—小结三段式？
- 有没有飘出「我懂你们不懂」的姿态？
- **本次输出自检（结构排比，双族都要扫）**：
  - 显性族：有没有用分号串联三个以上子句？段落标题有没有「N刀/N步/N条」量词序列或机械编号？
  - 隐性族：有没有「X 不 X」三连以上？有没有「不是 X 而是 Y」及其变体？有没有二连对仗（「就该 A，就该 B」/「不必 A，更不必 B」）超过一处？有没有段内三连罗列的名词/动词串？
- 标题过了红线九条吗？题眼是读者已知锚点（不是陌生术语）吗？
- 主线反思三问答完了吗？

任何一条答不上，回去补。
