---
name: haohao-shuohua
description: 好好说话 - 老羊的中文写作/说话底层 lint。管的是从聊天回复到硬文的一切中文输出,四条压顶原则:保事实、去 AI 味、加中文味、造词红线。三档场景:轻档常驻对话(回复消息、口语转书面);中档改手记周报(短稿保结构);重档改文档硬文(可结构性重写但走两遍回读)。核心特点:先锁事实账本再动刀,力度和 scope 两轴独立,长文默认 bounded 不擅自删整句、给建议清单交用户,改完自己回读三问是否偏离主线。触发词:去 AI 味、改中文、改稿、翻译腔、把这段说人话、帮我润一下、把这段改成中文的样子、这段听着像 AI、加中文味、保留原意重写、好好说话。DO NOT USE WHEN:纯翻译外文(走翻译流程)、生成新内容而非润色改写(走写作技能)、代码/命令/接口文档的技术改写(走对应技术技能)。淬火/汤山体/费曼三个写作技能在自己内部的「去 AI 味」环节应调用本技能重档。
---

# 好好说话 · 中文写作/说话底层 lint

## 这个技能是什么

老羊的中文输出防线。不管是回一句消息、写一段周报、还是打磨一篇硬文,凡是中文,都从这里过一遍。**它不是写作技能,是清洗技能**——不生成新内容,只让已有的中文回到「像人说的中文」。

一句话总纲:**保事实不动,去 AI 味,加中文味,不许造词充深刻。**

---

## 四条压顶原则(全档共享,顺序即优先级)

### 一、保事实(最高)

改中文的过程里,**原意、数字、命令、术语、谓词方向、效果类型、关系类型、硬机制描述、论文级证据一律不许动**。这是死线。听着再顺,把「压力大→辞职多」改成「辞职多→压力大」就是废稿。

**保真优先于加中文味**:「具体先于抽象」这类中文原则,在遇到硬机制/论文结论时让位——一个精确机制(如「attention 是 Q-K 相关度加权」)不许被「顶替成一个具体场景」,论文结论不许被简化成「有研究发现」。

事实闸门在下面「五段主流程」的第一步落地,靠**关系账本**兜底。

### 二、去 AI 味

翻译腔 + 表演腔的组合病。靠**症状库四层扫描**(标点/句式/词汇/结构)+ **回译测试**兜底做诊断,靠**减法五刀**动刀。清单见 `references/symptom-dictionary.md`。

### 三、加中文味

减完之后稿子会瘦一圈,但可能瘦成空壳。加法是把中文本来的写法补回来。四条原则:**动词发力、具体先于抽象、短句串珠、留白信任读者**。清单见 `references/chinese-four-principles.md`。

**减法有底,节奏无顶。** 前几条原则治的是一句之内的病,能靠清单机械做,有天花板;削干净顶多"没病",还不一定"好"。"好"落在句与句之间——长短错落、气口、调子,这一层清单管不了,只能靠**念出声**。AI 读不了声音,但"念出声"能落成一组字面代理指标(憋气句 / 句长单调 / 软尾串 / 调子单一),机器粗筛、人念终审。执行细则见 `references/rhythm-check.md`。**一条压顶的界:节奏只管顺不顺、憋不憋气,绝不管够不够口语——长句不是病,长句里没气口才是病,首选补气口不是拆句。**

### 四、造词红线

**能用现成词就用现成词,禁止为了显得凝练自造新词。** 这是老羊亲自定的死线——AI 造的词一眼假、看着深刻、读者一脸懵。

判定标准和自检流程见 `references/word-coining-checklist.md`。硬约束:一稿不超过一个自造词;必须造的地方,同段内必须用一句大白话把它翻译一次(「XX,说白了就是……」)。

---

## 附加红线(和四条原则同优先级,单独列出来是因为最容易翻车)

### 比喻红线(吸收自淬火,同管所有档位)

**比喻只有一个合法用途:把一个读者本来不理解的概念/理念讲明白。除此之外,一律不打比方。**

- 精确的东西直接说,不要比喻。「iOS 的动态性」「并发调度」「上下文窗口」「AST」这类有确切定义的专业概念,直接用它的准确表述——它是绝对的、确定的,不需要「就像天和地」「好比……」来形容。**用不准确换准确,就是降专业性。**
- 门槛型比喻一律删。凡是为了「让读者更好理解 / 降低阅读门槛」而加的比方,删。降门槛是费曼的活,不是好好说话的活。
- 比喻不承担论证。「这就好比……所以……」是拿类比顶替推理,回去把中间的机理补上。
- 合法的比喻也从省、不堆叠。全篇最多一两个精准的当锚点。

**例外**:费曼场景(讲给外行/自己搞懂)豁免——费曼技能内部对比喻的规则是「命根子」,和本条冲突时以费曼为准。判断:读者是同行(硬约束)还是外行(费曼豁免)?

### 反车轱辘话红线(吸收自淬火)

「一节一观点」是结构级、留给写作技能。**haohao-shuohua 只在句子级和段落级做删除**:

- **同义反复即删。** 一个意思说完就停,禁止「换个角度再说一遍」「正面说完反面再说」「举完例子再把例子的结论复述一遍」。判断:这句话删掉,读者对这个观点的理解会不会少一分?不会,就是车轱辘话。
- **过渡句从省。** 「顺着这个思路往下」「说到这里」「我们再看」这类纯连接、不带新信息的过渡,能删就删。**例外**:费曼场景的「那么/于是/但是/所以」是逻辑链的关节,不许删——费曼技能有明确要求。
- **禁止「铺垫—展开—小结」三段式。** 一句话能说清的判断,不要先预告我要讲什么、再讲、最后总结我讲了什么。直接讲。

### 姿态红线(吸收自淬火)

- **禁止**任何宣称式表达:「很多人没搞懂而我……」「大厂/硅谷都没想明白,但我……」「我早就看透了……」。
- **只做**:呈现观察、摆出机理、给出证据,让读者自己得出「这人想明白了」的结论。
- 平视。你和读者是同一战壕的人,不是你在台上讲课。

飘出一丝居高临下的味,前面所有硬货都打折。

---

## 三档场景(按触发方式和改动力度分)

| 档位 | 触发方式 | 适用场景 | 力度默认值 | Scope 默认值 |
|---|---|---|---|---|
| **轻档** | 常驻对话软自律 | Mira 回消息、口语转书面、群内回复、简短说明 | minimal | in-place |
| **中档** | 关键词触发 | 锻造手记、周报、日报、短说明文档 | standard | bounded |
| **重档** | 主动调用 / 被淬火·汤山·费曼等写作技能内部调用 | 硬文、对外文档、公众号稿、发布稿 | standard | bounded(可升 aggressive/structural,需说明) |

- **轻档不出现清单化 report**,直接给改后的文本。发现明显 AI 味悄悄改掉即可,不解释、不列诊断。
- **中档**每段级动刀,不擅自动结构。发现整段可删,列在末尾「建议删除清单」交老羊。
- **重档**允许在得到许可后做结构性重排,但也要走完整的**两遍回读**:Pass 1 只对事实,Pass 2 只查残留 AI 味,**Pass 2 不许再重写全文**。

**力度轴**(minimal / standard / aggressive):越大改得越狠,词句到结构逐级放开。
**Scope 轴**(in-place / bounded / structural):越大动的范围越大——in-place 只换词、bounded 可拆合句子、structural 可拆合段落甚至改章节顺序。

两轴独立配置。默认组合见上表。老羊可显式指定(「你放开点改」= aggressive;「只挑最刺眼的」= minimal + in-place)。

---

## 五段主流程(中档、重档必走;轻档并进心里做)

### 第一段:事实闸门(改前锁账本)

改任何一个字之前,先把**关系账本**列出来。账本项:

- **数字**:所有数字、单位、比例、时间、人名。
- **命令 / 代码 / 术语 / 专有名词**:凡是引号、代码块、`code` 包住的、以及首字母大写的英文术语,视为不可修改的**受保护跨度**(protected span,详见 `references/protected-spans.md`)。
- **谓词方向**:「A 导致 B」不能变成「B 导致 A」;「A 大于 B」不能变成「A 小于 B」;「A 属于 B」不能变成「B 属于 A」。
- **效果类型**:上升 / 下降 / 不变 / 存在 / 消失,不许翻转。
- **关系类型**:因果 / 并列 / 转折 / 递进 / 让步,不许换。
- **模态**:「可能」「一定」「大概率」不许升级或降级。
- **硬机制描述**:「Q-K 点积加权求和」「反向传播」「AST 遍历」这类精确算法/机制描述,不许替换成比喻或简化说法。想加比喻,叠在机制后面(汤山体保真层的做法),不许替换。
- **论文级证据**:「Kenneth Li 等在 Othello-GPT 里发现……(ICLR 2023)」这类可追溯的实验描述,作者/机构/年份/结论不许被简化成「有研究发现」「科学家表示」。

账本用几行 checklist 记住(或写到暂存区)。改完最后一步逐条对回去。**任何一条对不上,该句作废,回去重改**。

### 第二段:症状库四层扫描 + 回译兜底

按 `references/symptom-dictionary.md` 逐层扫描,标注命中项:

- **标点级**:破折号、引号里的抽象词、括号英文注释。
- **句式级**:「不是 X 而是 Y」、「更是也是」、「前者后者」、「值得深思」、「我们不禁要问」。
- **词汇级**:「XX 化 / 性 / 型 / 度」后缀病、「XX 这一环节」冗余名词化、「至关重要」强调副词、「进行/实现/完成 + 名词」名词化动词。
- **结构级**:段末打钉、逢三凑数、过度均衡。

标注完密度先自检:**某段命中 > 5 处 → 该段整体不对,单点改不救,直接考虑重写(重档)或整段列删除清单(中档)**。

**回译测试兜底**:任何一句拿不准是不是 AI 味,默读一遍翻回英文。英文一次成型 → AI 味;英文别扭需要意译 → 中文。

### 第三段:力度 × Scope 动刀

按上一段扫出来的密度和当前档位的默认力度/scope 配置,开始下刀:

- **五刀减法**(见 `references/symptom-dictionary.md`「减法五刀」节):名词化 → 动词;后缀病 → 删或换;破折号 → 逗号/句号;前者后者 → 重复主语;长复合句 → 短句串珠。
- **四问加法**(见 `references/chinese-four-principles.md`):动词扛住了没?有具体没?句子拆开没?段末多话删了没?

**Scope = bounded 时的硬约束**:
- 不擅自删整句。发现某句该删,记进**「建议删除清单」**,主流程末尾统一交老羊。
- 结构级重排(合并两段、拆分一段、调整章节顺序)必须先问,不许直接做。

**Scope = structural 时**:结构级重排可自主做,但每次都要在 report 里说明「原来是……,改后是……,因为……」。

### 第四段:两遍回读(重档强制;中档抽查)

改完不许直接交。分两遍念:

- **Pass 1 · 只对事实**:拿第一段的关系账本,一条一条对回去。**只查、不改结构,不查行文**。发现任何一条对不上,回到第三段重改该处。
- **Pass 2 · 只查残留 AI 味**:整篇再念一遍,只找漏网的四层症状。**发现问题只做局部替换,不许借机重写全文**——这一步的目的是收尾,不是再来一遍。
- **Pass 3 · 节奏终审(重档强制,中档只跑憋气句)**:按 `references/rhythm-check.md` 的四个字面指标粗筛,把憋气句 / 句长单调 / 软尾串 / 调子单一的地方标红,逐处念一遍定夺。**只补气口、只微调句式,不推向口语化,不拆好长句。** 误报(URL、排比、专名列表)放过。

三遍分开做,不许合并。合并的结果就是「越改越顺、越顺越不像原稿」,最终跑题。

### 第五段:主线反思(软性,不强制约束)

改完 skill 自己回读全文,回答三问:

1. **主线是什么?** 用一句话讲清整篇的核心主张。讲不出来 → 主线已散,需要老羊定夺是否重来。
2. **每一节还在为它服务吗?** 逐节问「删掉这节,主线还立得住吗?立得住 → 这节是废段,标出来。」
3. **比改之前更清楚,还是更散?** 只跟改前比,不跟理想比。如果改后反而更散(常见于补丁摞补丁),把偏离点标出来交老羊判断,**不擅自回滚**。

三问的答案跟着 report 一起给。**不当硬约束**——老羊说过,有时候就是想改结构、想让主线松一些,不能限死。这一步只做提醒。

---

## 标题红线(单独一节,吸收自淬火 skill,同管主标题和章节小标题)

标题是最容易露 AI 味 / 公众号味的地方。总要求:**平实、深刻,是什么就写什么。直给的最好——把判断直接摆在标题里,让读者一眼看到结论,而不是被勾着点进去猜。**

### 硬规则(踩一条就重起)

0. **直给优先(最高)。** 能一句话把结论摆出来就摆出来,不绕、不藏、不卖关子。
1. **禁破折号。** 标题里出现「——」基本就是 AI 味,零容忍。
2. **禁超长。** 一行、约 20 字以内为宜。要用三四个分句串起来的,回去砍。
3. **禁悬念钩子。** 不写「我把它拆开看了」「你绝对想不到」「真相是」「背后的秘密」。
4. **禁「不是 X,而是 Y」。** 头号 AI 味句式,标题零容忍。
5. **禁三连罗列。** 「A、B、C」三名词堆成标题 = 逢三凑数无立场。
6. **一句陈述 + 一个判断。** 好标题落地成一句平实陈述句,里面藏一个可被反驳的判断。
7. **章节标题不搞机械编号。** 不用「一、二、三」硬编号,每个小标题自己就是一句能立住的话。
8. **标题也做回译测试。** 翻回英文一次成型的标题基本是翻译腔。
9. **题眼落在读者已知的锚点上,别用陌生术语当题眼。** 这条最容易被忽略,却最要命。好奇心不是「你不知道什么」勾起来的,是「你已经知道一点」勾起来的——读者手里得先有个锚,才会想往下够。标题里塞一个读者完全没听过的词(专有名词、自造概念、生僻术语),缺口太大,读者根本无从好奇,直接划走。修法:把陌生术语从题眼上撤下来,换成全文已经反复建立、读者一眼认得的那个概念,让新东西留到正文里揭。判断:标题里的关键词,读者不点进来能不能懂?不能懂→换锚点。

### 信达雅落到标题上

严复那三个字,搬到标题上就是三个具体动作:

- **信 = 名副其实,不骗点击。** 标题许诺的,正文必须给。掐头去尾、制造悬念却不给干货,是标题党,读者会脱敏。
- **达 = 一眼看懂,不设门槛。** 关键词是读者已知的锚点(见硬规则 9),句子是平实陈述句,读者扫一眼就 get 到你在说什么。
- **雅 = 有余味,留一个恰好的缺口。** 不是辞藻华丽,是在「全懂」和「全不懂」之间留一道缝——读者懂了大半,还差一口,那口气就把他领进正文。缺口开太大(陌生术语)读者放弃,开太小(把结论说尽)读者不必点。

三者不冲突:先信(不骗)、再达(看懂)、后雅(留缝)。达和雅打架时,达优先——宁可直白,不要为了余味把话说得读者看不懂。

### 章节小标题最高发区

主标题往往改得干净,**章节小标题却全是「XX 那点别扭,根子是同一个」「这条路的尽头,是一个新深渊」这类卖关子的公众号句式**。小标题不是钩子,它就是这一节观点的**直接陈述**,别设扣子。

### 标杆与反例

**对(标杆)**:
- 「AI 打开 iOS 工程,和打开一个 txt 没区别」
- 「集体降智:把 AI 当外包,等于交出控制权」
- 「AI 写 iOS 为什么质量还是不太行」(朴素直问,不是设扣子的假提问)
- 「『AI 味』的病根,一百年前就诊断过」(题眼是「病根」——全文一直把 AI 味当病讲,读者手里已有「病」这个锚,顺势就想问「根在哪」;缺口恰好)

**错(要改)**:
- 「AI 说的中文,是欧化中文的工业版」——「是 X 的 Y 版」也是英文骨架 + 略微钩子味。
- 「AI 味就是欧化中文,一百年前就诊断过」——曾经以为这是标杆,其实踩了硬规则 9:「欧化中文」是读者陌生的术语,当题眼没人 get 得到,缺口太大直接划走。把陌生术语撤下来、换成读者已知的锚点「病根」,才成立(见上「对」的最后一条)。这条对照最能说明问题:同一篇文章,只换了题眼落在哪个词上,标题就从「读者读不懂」变成「读者想读下去」。
- 「意图债、理解债、认知投降」——三连名词无判断
- 「这条路的尽头,是一个新深渊」——纯钩子

---

## Report 模板(中档、重档必给;轻档不给)

改完的输出结构:

```
【改后正文】
<改完的中文>

【事实核对】
- 关系账本 N 条,全部对齐 ✓
(如有对不上,说明该处未改动或回退原状)

【本次动刀】
- 减法五刀命中:XX 处(简要 1 行)
- 加法四问补充:XX 处(简要 1 行)

【建议删除清单】(bounded scope 独有)
- 第 N 段第 M 句「……」建议删,理由:段末打钉/同义反复/凑三/……

【主线反思】
- 主线一句话:……
- 有无偏离:无 / 有(说明哪一节偏了)
- 比改前:更清楚 / 更散(简答)
```

轻档只给改后正文,不给这套 report。

---

## 与其他 skill 的关系

- **淬火(想法硬文)、汤山体(畅想)、费曼(技术调研)**:它们各自的「去 AI 味 / 标题红线」环节,在写完之后调用**本技能重档**做统一 lint。避免每个 skill 各自维护一份不同步的 AI 味清单。
- **锻造手记**:淬火产物之一,写完短思考走本技能轻档过一遍。
- **独立调用**:老羊直接说「把这段用好好说话过一下」/「这段听着像 AI,改一下」/「帮我把这周周报润一下」→ 直接进本技能,按语境挑档位。

---

## 使用流程速查

1. **判档**:对话回复 → 轻档;手记/周报/短稿 → 中档;硬文/对外文档 → 重档。
2. **锁账本**:列关系账本 6 项(数字/命令/谓词方向/效果/关系/模态)。
3. **扫症状**:症状库四层扫一遍,标密度。密度 > 5/段 → 建议重写或整段进删除清单。
4. **动刀**:按档位默认力度和 scope,减法五刀 + 加法四问。bounded 下不删整句、不动结构。
5. **回读**:Pass 1 对账本、Pass 2 查残留、Pass 3 跑节奏(憋气句/句长单调/软尾串/调子单一,只补气口不口语化)。
6. **反思三问**:主线是什么?节节服务吗?比改前清楚吗?
7. **出稿 + report**(中/重档);轻档只出正文。

---

## 自检:出稿前问自己

- 事实账本对齐了吗(含硬机制/论文证据)?
- 减法五刀命中的都动刀了吗?
- 加法四问答完了吗?
- **节奏跑过了吗?有没有一口气顶到底的憋气句没补气口?(注意:补气口,别拆好长句、别改口语)**
- 有没有偷偷造新词?
- **比喻是不是都在讲清难懂概念,没有一个在给精确概念套壳降门槛?**
- **有没有同义反复、废过渡句、铺垫—小结三段式?**
- **有没有飘出「我懂你们不懂」的姿态?**
- 标题过了红线九条了吗?题眼是不是读者已知的锚点(不是陌生术语)?
- 主线反思三问答完了吗?

任何一条答不上,回去补。
